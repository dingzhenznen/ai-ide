# Asset notes

This folder is for UI images used by the renderer.

App / installer icons are built from `build/icon.png` and committed as:
- `build/icon.icns` (macOS)
- `build/icon.ico` (Windows)
- `build/icon.png` (source / Linux)

